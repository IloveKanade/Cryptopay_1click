# 中间人钱包管理功能测试

## 🎯 功能概述

中间人钱包管理页面提供了完整的中间人钱包管理功能，包括：

1. **钱包管理** - 添加、删除、启用/禁用中间人钱包
2. **转账记录** - 查看所有转账记录和状态
3. **费率配置** - 设置和管理手续费率

## 🚀 访问方式

1. **启动系统**:
   ```bash
   start.bat
   ```

2. **管理员登录**:
   - 邮箱: admin@example.com
   - 密码: admin123

3. **访问中间人钱包管理**:
   - 方式1: 在管理后台点击"中间人钱包"导航
   - 方式2: 直接访问 http://localhost:3000/admin/middleman

## 📋 功能测试

### 1. 钱包管理测试

#### 添加钱包
1. 点击"添加钱包"按钮
2. 填写钱包名称（如：测试钱包1）
3. 填写TRC20钱包地址（如：TQn9Y2khDD95J42FQtQTdwVVRj2qE8qXK1）
4. 点击"添加"按钮
5. 验证钱包出现在列表中

#### 启用/禁用钱包
1. 在钱包列表中，使用开关切换钱包状态
2. 验证状态标签正确显示（启用/禁用）

#### 删除钱包
1. 点击钱包行的删除按钮
2. 确认删除操作
3. 验证钱包从列表中移除

### 2. 转账记录测试

#### 查看转账记录
1. 切换到"转账记录"标签页
2. 查看转账记录列表
3. 验证显示信息包括：
   - 订单ID
   - 来源地址
   - 目标地址
   - 金额
   - 手续费
   - 状态
   - 创建时间

### 3. 费率配置测试

#### 创建费率配置
1. 切换到"费率配置"标签页
2. 点击"创建配置"按钮
3. 设置手续费率（如：1%）
4. 设置最小手续费（如：0.1 USDT）
5. 设置最大手续费（如：10 USDT）
6. 点击"创建"按钮

#### 编辑费率配置
1. 点击"编辑配置"按钮
2. 修改费率参数
3. 点击"更新"按钮
4. 验证配置更新成功

## 🔧 API接口测试

### 钱包管理API
```bash
# 获取钱包列表
GET http://localhost:8080/api/admin/middleman-wallets

# 添加钱包
POST http://localhost:8080/api/admin/middleman-wallets
{
  "name": "测试钱包",
  "address": "TQn9Y2khDD95J42FQtQTdwVVRj2qE8qXK1"
}

# 更新钱包状态
PUT http://localhost:8080/api/admin/middleman-wallets/1/status
{
  "status": "active"
}

# 删除钱包
DELETE http://localhost:8080/api/admin/middleman-wallets/1
```

### 转账记录API
```bash
# 获取转账记录
GET http://localhost:8080/api/admin/transfer-records

# 获取单个转账记录
GET http://localhost:8080/api/admin/transfer-records/1
```

### 费率配置API
```bash
# 获取费率配置
GET http://localhost:8080/api/admin/fee-config

# 创建费率配置
POST http://localhost:8080/api/admin/fee-config
{
  "fee_rate": 1.0,
  "min_fee": 0.1,
  "max_fee": 10.0
}

# 更新费率配置
PUT http://localhost:8080/api/admin/fee-config
{
  "fee_rate": 1.5,
  "min_fee": 0.2,
  "max_fee": 15.0
}
```

## 🎨 界面特性

### 响应式设计
- 桌面端：完整表格显示
- 移动端：适配移动设备

### 状态指示
- 钱包状态：启用（绿色）/ 禁用（红色）
- 转账状态：处理中（黄色）/ 已完成（绿色）/ 失败（红色）

### 交互功能
- 实时状态切换
- 确认删除对话框
- 表单验证
- 错误提示

## ⚠️ 注意事项

1. **钱包地址格式**：必须使用有效的TRC20钱包地址
2. **费率范围**：手续费率应在0-100%之间
3. **权限控制**：只有管理员可以访问此页面
4. **数据持久化**：所有操作都会保存到数据库

## 🐛 故障排除

### 常见问题

1. **页面无法访问**
   - 检查是否以管理员身份登录
   - 确认后端服务正常运行

2. **API调用失败**
   - 检查网络连接
   - 查看浏览器控制台错误信息
   - 确认API地址正确

3. **数据不显示**
   - 检查数据库连接
   - 确认数据表已创建
   - 查看后端日志

### 调试方法

1. **前端调试**：
   - 打开浏览器开发者工具
   - 查看Console和Network标签页

2. **后端调试**：
   - 查看Docker容器日志
   - 检查数据库连接状态

3. **数据库调试**：
   - 连接MySQL数据库
   - 查询相关表数据

## 📈 性能优化

1. **数据加载**：使用并行请求获取数据
2. **状态管理**：本地状态缓存减少重复请求
3. **错误处理**：友好的错误提示和重试机制
4. **用户体验**：加载状态指示和操作反馈
